Clear the cruxdev session state for the current Claude Code conversation.

If your goal is to wipe the conversation history, use the built-in `/clear` instead — that is what Claude Code provides for resetting context.

If your goal is to clear cruxdev-side state (chatflow turn log, in-flight convergence, claim locks, heartbeat files), confirm what should be cleared and run the appropriate cruxdev MCP tool or shell cleanup. Do not infer; ask first.
