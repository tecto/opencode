# /patterns-check

This slash command is a placeholder. It exists in `.claude/commands/` but has no body yet.

To implement it, replace this file with the actual prompt or instructions you want Claude Code to execute when `/patterns-check` is invoked.

(Auto-stubbed 2026-04-30 to prevent the empty-slash-command bug that injects empty isMeta user messages and trips the Anthropic API "messages: text content blocks must be non-empty" validation.)
